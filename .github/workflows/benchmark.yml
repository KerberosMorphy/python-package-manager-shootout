# DO NOT EDIT MANUALLY
# This file is generated by ./bin/build_workflow.sh
---
name: benchmark
on:
  workflow_dispatch:
  
env:
  GL_USERNAME: ${{ secrets.GL_USERNAME }}
  GLPAT_GOBBLET: ${{ secrets.GLPAT_GOBBLET }}
  GLPAT_QUORIDOR: ${{ secrets.GLPAT_QUORIDOR }}

jobs:
  poetry:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"

      - name: setup
        run: ./bin/actions_prereqs.sh

      - name: tooling
        run: |
          /usr/bin/time --output=timings/tooling.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make poetry-tooling

      - name: import
        run: |
          /usr/bin/time --output=timings/import.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make poetry-import

      - name: lock
        run: |
          make poetry-clean-cache
          make poetry-clean-venv
          make poetry-clean-lock
          /usr/bin/time --output=timings/lock.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make poetry-lock

      - name: install cold
        run: |
         make poetry-clean-cache
         make poetry-clean-venv
         /usr/bin/time --output=timings/install-cold.txt --format="%e,%S,%U,%P,%M,%I,%O" \
           make poetry-install

      - name: install warm
        run: |
          make poetry-clean-venv
          /usr/bin/time --output=timings/install-warm.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make poetry-install

      - name: update
        run: |
          /usr/bin/time --output=timings/update.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make poetry-update

      - name: add package
        run: |
          /usr/bin/time --output=timings/add-package.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make poetry-add-package

      - name: stats
        run: |
          VERSION=$(make poetry-version)
          CSV=poetry/stats.csv
          TIMESTAMP=$(date +%s)
          mkdir -p "poetry"
          echo "tool,version,timestamp,stat,elapsed time,system,user,cpu percent,max rss,inputs,outputs" > "$CSV"
          for stat in "tooling" "import" "lock" "install-cold" "install-warm" "update" "add-package"; do
            echo "poetry,$VERSION,$TIMESTAMP,$stat,$(cat timings/$stat.txt | tr -d '%')" >> "$CSV"
          done
          mdtable "$CSV" >> $GITHUB_STEP_SUMMARY

      - uses: actions/upload-artifact@v3
        if: github.ref == 'refs/heads/main'
        with:
          name: poetry-stats
          path: poetry/stats.csv
          retention-days: 10

  pdm-582:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"

      - name: setup
        run: ./bin/actions_prereqs.sh

      - name: tooling
        run: |
          /usr/bin/time --output=timings/tooling.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-582-tooling

      - name: import
        run: |
          /usr/bin/time --output=timings/import.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-582-import

      - name: lock
        run: |
          make pdm-582-clean-cache
          make pdm-582-clean-venv
          make pdm-582-clean-lock
          /usr/bin/time --output=timings/lock.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-582-lock

      - name: install cold
        run: |
         make pdm-582-clean-cache
         make pdm-582-clean-venv
         /usr/bin/time --output=timings/install-cold.txt --format="%e,%S,%U,%P,%M,%I,%O" \
           make pdm-582-install

      - name: install warm
        run: |
          make pdm-582-clean-venv
          /usr/bin/time --output=timings/install-warm.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-582-install

      - name: update
        run: |
          /usr/bin/time --output=timings/update.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-582-update

      - name: add package
        run: |
          /usr/bin/time --output=timings/add-package.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-582-add-package

      - name: stats
        run: |
          VERSION=$(make pdm-582-version)
          CSV=pdm-582/stats.csv
          TIMESTAMP=$(date +%s)
          mkdir -p "pdm-582"
          echo "tool,version,timestamp,stat,elapsed time,system,user,cpu percent,max rss,inputs,outputs" > "$CSV"
          for stat in "tooling" "import" "lock" "install-cold" "install-warm" "update" "add-package"; do
            echo "pdm-582,$VERSION,$TIMESTAMP,$stat,$(cat timings/$stat.txt | tr -d '%')" >> "$CSV"
          done
          mdtable "$CSV" >> $GITHUB_STEP_SUMMARY

      - uses: actions/upload-artifact@v3
        if: github.ref == 'refs/heads/main'
        with:
          name: pdm-582-stats
          path: pdm-582/stats.csv
          retention-days: 10

  pdm-venv:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"

      - name: setup
        run: ./bin/actions_prereqs.sh

      - name: tooling
        run: |
          /usr/bin/time --output=timings/tooling.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-venv-tooling

      - name: import
        run: |
          /usr/bin/time --output=timings/import.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-venv-import

      - name: lock
        run: |
          make pdm-venv-clean-cache
          make pdm-venv-clean-venv
          make pdm-venv-clean-lock
          /usr/bin/time --output=timings/lock.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-venv-lock

      - name: install cold
        run: |
         make pdm-venv-clean-cache
         make pdm-venv-clean-venv
         /usr/bin/time --output=timings/install-cold.txt --format="%e,%S,%U,%P,%M,%I,%O" \
           make pdm-venv-install

      - name: install warm
        run: |
          make pdm-venv-clean-venv
          /usr/bin/time --output=timings/install-warm.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-venv-install

      - name: update
        run: |
          /usr/bin/time --output=timings/update.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-venv-update

      - name: add package
        run: |
          /usr/bin/time --output=timings/add-package.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pdm-venv-add-package

      - name: stats
        run: |
          VERSION=$(make pdm-venv-version)
          CSV=pdm-venv/stats.csv
          TIMESTAMP=$(date +%s)
          mkdir -p "pdm-venv"
          echo "tool,version,timestamp,stat,elapsed time,system,user,cpu percent,max rss,inputs,outputs" > "$CSV"
          for stat in "tooling" "import" "lock" "install-cold" "install-warm" "update" "add-package"; do
            echo "pdm-venv,$VERSION,$TIMESTAMP,$stat,$(cat timings/$stat.txt | tr -d '%')" >> "$CSV"
          done
          mdtable "$CSV" >> $GITHUB_STEP_SUMMARY

      - uses: actions/upload-artifact@v3
        if: github.ref == 'refs/heads/main'
        with:
          name: pdm-venv-stats
          path: pdm-venv/stats.csv
          retention-days: 10
  
  pipenv:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"

      - name: setup
        run: ./bin/actions_prereqs.sh

      - name: tooling
        run: |
          /usr/bin/time --output=timings/tooling.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pipenv-tooling

      - name: import
        run: |
          /usr/bin/time --output=timings/import.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pipenv-import

      - name: lock
        run: |
          make pipenv-clean-cache
          make pipenv-clean-venv
          make pipenv-clean-lock
          /usr/bin/time --output=timings/lock.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pipenv-lock

      - name: install cold
        run: |
         make pipenv-clean-cache
         make pipenv-clean-venv
         /usr/bin/time --output=timings/install-cold.txt --format="%e,%S,%U,%P,%M,%I,%O" \
           make pipenv-install

      - name: install warm
        run: |
          make pipenv-clean-venv
          /usr/bin/time --output=timings/install-warm.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pipenv-install

      - name: update
        run: |
          /usr/bin/time --output=timings/update.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pipenv-update

      - name: add package
        run: |
          /usr/bin/time --output=timings/add-package.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pipenv-add-package

      - name: stats
        run: |
          VERSION=$(make pipenv-version)
          CSV=pipenv/stats.csv
          TIMESTAMP=$(date +%s)
          mkdir -p "pipenv"
          echo "tool,version,timestamp,stat,elapsed time,system,user,cpu percent,max rss,inputs,outputs" > "$CSV"
          for stat in "tooling" "import" "lock" "install-cold" "install-warm" "update" "add-package"; do
            echo "pipenv,$VERSION,$TIMESTAMP,$stat,$(cat timings/$stat.txt | tr -d '%')" >> "$CSV"
          done
          mdtable "$CSV" >> $GITHUB_STEP_SUMMARY

      - uses: actions/upload-artifact@v3
        if: github.ref == 'refs/heads/main'
        with:
          name: pipenv-stats
          path: pipenv/stats.csv
          retention-days: 10

  pip-tools:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"

      - name: setup
        run: ./bin/actions_prereqs.sh

      - name: tooling
        run: |
          /usr/bin/time --output=timings/tooling.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pip-tools-tooling

      - name: import
        run: |
          /usr/bin/time --output=timings/import.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pip-tools-import

      - name: lock
        run: |
          make pip-tools-clean-cache
          make pip-tools-clean-venv
          make pip-tools-clean-lock
          /usr/bin/time --output=timings/lock.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pip-tools-lock

      - name: install cold
        run: |
         make pip-tools-clean-cache
         make pip-tools-clean-venv
         /usr/bin/time --output=timings/install-cold.txt --format="%e,%S,%U,%P,%M,%I,%O" \
           make pip-tools-install

      - name: install warm
        run: |
          make pip-tools-clean-venv
          /usr/bin/time --output=timings/install-warm.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pip-tools-install

      - name: update
        run: |
          /usr/bin/time --output=timings/update.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pip-tools-update

      - name: add package
        run: |
          /usr/bin/time --output=timings/add-package.txt --format="%e,%S,%U,%P,%M,%I,%O" \
            make pip-tools-add-package

      - name: stats
        run: |
          VERSION=$(make pip-tools-version)
          CSV=pip-tools/stats.csv
          TIMESTAMP=$(date +%s)
          mkdir -p "pip-tools"
          echo "tool,version,timestamp,stat,elapsed time,system,user,cpu percent,max rss,inputs,outputs" > "$CSV"
          for stat in "tooling" "import" "lock" "install-cold" "install-warm" "update" "add-package"; do
            echo "pip-tools,$VERSION,$TIMESTAMP,$stat,$(cat timings/$stat.txt | tr -d '%')" >> "$CSV"
          done
          mdtable "$CSV" >> $GITHUB_STEP_SUMMARY

      - uses: actions/upload-artifact@v3
        if: github.ref == 'refs/heads/main'
        with:
          name: pip-tools-stats
          path: pip-tools/stats.csv
          retention-days: 10

  gather:
    runs-on: ubuntu-latest
    needs: [poetry, pdm-582, pdm-venv, pipenv, pip-tools]
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/download-artifact@v3
      - name: build stats.csv
        run: |
          # join and remove duplicate headers
          cat *-stats/stats.csv > raw.csv
          HEADER=$(head -n 1 raw.csv)
          echo "$HEADER" > stats.csv
          grep -v "$HEADER" raw.csv >> stats.csv
      - uses: actions/upload-artifact@v3
        with:
          name: stats
          path: stats.csv
          retention-days: 10
